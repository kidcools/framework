server:
  port: 8086
spring:
  application:
    name: n-f-sb-mp-dr2
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
    #默认日期格式转换
    jackson:
      date-format: yyyy-MM-dd HH:mm:ss
      time-zone: GMT+8
      default-property-inclusion: always